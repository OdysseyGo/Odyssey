import { StyleSheet } from 'react-native';
import Colors, { ThemeName } from '@/constants/Colors';

export const puzzleHintStyles = (theme: ThemeName) => {
  return StyleSheet.create({
    container: {
      // Add any specific container styles if needed, currently empty as it wraps StoryInputField
    },
  });
};
